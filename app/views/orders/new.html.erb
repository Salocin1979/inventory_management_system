<!-- app/views/orders/new.html.erb -->
<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">New Order</h1>
    <%= link_to "Back to Orders", orders_path, class: "text-indigo-600 hover:text-indigo-900" %>
  </div>

  <%= render 'form', order: @order %>
</div>

<!-- app/views/orders/edit.html.erb -->
<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Edit Order #<%= @order.order_number %></h1>
    <%= link_to "Back to Order", order_path(@order), class: "text-indigo-600 hover:text-indigo-900" %>
  </div>

  <%= render 'form', order: @order %>
</div>
